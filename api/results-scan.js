const {API_KEY,loadMatches,isIpbl,matchToRows,rawInfo}=require('./_lib');
module.exports=async(req,res)=>{res.setHeader('Content-Type','application/json');try{if(!API_KEY)return res.status(500).json({ok:false,error:'API_SPORT_KEY не задан'});const date=String(req.query.date||'').slice(0,10);const l=await loadMatches(date,{live:false});let bank=Number(req.query.bank||10000);const rows=[],raw=[];for(const m of l.matches){const r=matchToRows(m);if(r.length)rows.push(...r);else if(isIpbl(m))raw.push(rawInfo(m))}const results=rows.sort((a,b)=>String(a.time).localeCompare(String(b.time))||a.quarter-b.quarter).map(r=>{const profit=r.passed?375:-500;bank+=profit;return{...r,odds:1.75,stake:500,profit,bankAfter:bank}});res.status(200).json({ok:true,apiMatches:l.matches.length,count:results.length,results,rawMatches:raw,diagnostics:{slugs:l.slugs,attempts:l.attempts}})}catch(e){res.status(e.status||500).json({ok:false,error:e.message,data:e.data})}};
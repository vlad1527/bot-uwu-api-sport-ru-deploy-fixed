const {API_KEY,loadMatches}=require('./_lib');
module.exports=async(req,res)=>{try{if(!API_KEY)return res.status(500).json({ok:false,error:'API_SPORT_KEY не задан'});const date=String(req.query.date||'').slice(0,10)||new Date().toISOString().slice(0,10);const l=await loadMatches(date,{live:false});res.status(200).json({ok:true,totalMatches:l.matches.length,uniqueLeagues:0,diagnostics:{slugs:l.slugs,attempts:l.attempts}})}catch(e){res.status(e.status||500).json({ok:false,error:e.message,data:e.data})}};